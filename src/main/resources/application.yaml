spring:
  datasource:
    username: user
    password: password
    url: jdbc:postgresql://localhost:5432/ManageMate3
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

camunda:
  client:
    zeebe:
      base-url: http://localhost:26500